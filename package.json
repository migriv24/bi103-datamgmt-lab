{
  "name": "bi103-datamgmt-lab",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "chart.js": "^4.4.0",
    "chartjs-adapter-date-fns": "^3.0.0",
    "date-fns": "^3.0.0"
  },
  "devDependencies": {
    "vite": "^8.0.0",
    "gh-pages": "^6.0.0"
  }
}
